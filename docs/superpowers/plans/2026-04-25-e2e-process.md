# E2E Process — Flux Contact → Onboarding → Livraison

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-extended-cc:subagent-driven-development (recommended) or superpowers-extended-cc:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Connecter le formulaire vitrine à Supabase + Resend via Vercel Edge Functions, puis câbler le wizard d'onboarding et l'email de livraison pour un flux E2E complet prospect → client livré.

**Architecture:** Trois Vercel Edge Functions (`/api/contact`, `/api/onboarding`, `/api/delivery`) reçoivent les données des formulaires, les insèrent dans Supabase (tables `leads` et `projets`) et déclenchent les emails via Resend. Le formulaire vitrine existant est câblé sur `/api/contact`. Le wizard onboarding existant est câblé sur `/api/onboarding`. La livraison est déclenchée par un appel manuel du PM.

**Tech Stack:** Vanilla JS, Vercel Edge Functions (Node.js ESM), Supabase REST API (fetch natif, pas de client SDK), Resend API.

**User Verification:** NO — pas de vérification utilisateur requise mid-plan. Le plan de test E2E à l'étape 7 permet au PM de valider lui-même.

---

## Fichiers créés / modifiés

| Fichier | Action | Responsabilité |
|---------|--------|----------------|
| `wedoria-site-vitrine/api/contact.js` | Créer | Edge Function — reçoit lead, insère Supabase, envoie email PM |
| `wedoria-site-vitrine/api/onboarding.js` | Créer | Edge Function — reçoit brief, insère projet, envoie email client |
| `wedoria-site-vitrine/api/delivery.js` | Créer | Edge Function — marque projet livré, envoie email confirmation |
| `wedoria-site-vitrine/index.html` | Modifier | Câbler le formulaire contact sur `/api/contact` |
| `wedoria-site-vitrine/onboarding/index.html` | Modifier | Câbler le wizard sur `/api/onboarding` |
| `wedoria-site-vitrine/.env.example` | Créer | Documentation des variables d'environnement |
| `wedoria-site-vitrine/vercel.json` | Modifier | Ajouter `functions` config si nécessaire |

---

### Task 0 : Créer les tables Supabase

**Goal:** Créer les tables `leads` et `projets` dans Supabase via le SQL Editor.

**Files:**
- Create: `wedoria-site-vitrine/.env.example`

**Acceptance Criteria:**
- [ ] Table `leads` créée avec les colonnes définies dans la spec
- [ ] Table `projets` créée avec FK vers `leads`
- [ ] Row Level Security désactivé (accès via service key uniquement)
- [ ] `.env.example` documenté avec toutes les variables

**Verify:** Ouvrir Supabase → Table Editor → vérifier que `leads` et `projets` apparaissent

**Steps:**

- [ ] **Step 1 : Ouvrir le SQL Editor Supabase et exécuter**

```sql
-- Table leads
CREATE TABLE leads (
  id          UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  created_at  TIMESTAMPTZ DEFAULT now(),
  prenom1     TEXT NOT NULL,
  prenom2     TEXT,
  email       TEXT NOT NULL,
  telephone   TEXT,
  date_mariage DATE,
  message     TEXT,
  statut      TEXT DEFAULT 'nouveau'
    CHECK (statut IN ('nouveau', 'contacté', 'proposition', 'signé', 'archivé'))
);

-- Table projets
CREATE TABLE projets (
  id                UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  created_at        TIMESTAMPTZ DEFAULT now(),
  lead_id           UUID REFERENCES leads(id),
  prenom1           TEXT NOT NULL,
  prenom2           TEXT,
  email_client      TEXT NOT NULL,
  date_mariage      DATE,
  template          TEXT CHECK (template IN ('classique', 'romantique', 'chic')),
  formule           TEXT CHECK (formule IN ('essentielle', 'premium', 'sur-mesure')),
  couleur_primaire  TEXT,
  couleur_secondaire TEXT,
  police            TEXT,
  lieu              TEXT,
  programme         TEXT,
  infos_pratiques   TEXT,
  statut            TEXT DEFAULT 'brief'
    CHECK (statut IN ('brief', 'en-cours', 'livré', 'archivé')),
  url_site          TEXT,
  livre_at          TIMESTAMPTZ
);
```

- [ ] **Step 2 : Récupérer les credentials Supabase**

Dans Supabase → Settings → API :
- Copier `Project URL` → valeur de `SUPABASE_URL`
- Copier `service_role` key (secret) → valeur de `SUPABASE_SERVICE_KEY`

- [ ] **Step 3 : Créer `.env.example`**

```
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGc...
RESEND_API_KEY=re_xxxx
PM_EMAIL=vu.nhusao@gmail.com
FROM_EMAIL=contact@wedoria.fr
```

- [ ] **Step 4 : Créer `.env.local` (jamais commité) avec les vraies valeurs**

Copier `.env.example` → `.env.local`, remplir les valeurs réelles.

- [ ] **Step 5 : Vérifier que `.env.local` est dans `.gitignore`**

```bash
cat .gitignore | grep .env
```
Si absent : ajouter `.env.local` dans `.gitignore`.

- [ ] **Step 6 : Commit**

```bash
git add .env.example .gitignore
git commit -m "chore: add env example for E2E setup"
```

```json:metadata
{"files": ["wedoria-site-vitrine/.env.example"], "verifyCommand": "", "acceptanceCriteria": ["tables leads et projets créées dans Supabase", ".env.example documenté"], "requiresUserVerification": false}
```

---

### Task 1 : Créer `/api/contact.js`

**Goal:** Edge Function qui reçoit le formulaire vitrine, insère un lead dans Supabase, et envoie un email de notification au PM.

**Files:**
- Create: `wedoria-site-vitrine/api/contact.js`

**Acceptance Criteria:**
- [ ] POST avec `prenom1` + `email` → 200 + `{ success: true, lead_id }`
- [ ] POST sans `email` → 400 + `{ error: "email requis" }`
- [ ] Lead inséré dans `leads` avec statut `'nouveau'`
- [ ] Email reçu par le PM dans les 30 secondes

**Verify:** `curl -X POST https://wedoria.vercel.app/api/contact -H "Content-Type: application/json" -d '{"prenom1":"Test","email":"test@test.com"}' → {"success":true}`

**Steps:**

- [ ] **Step 1 : Créer `wedoria-site-vitrine/api/contact.js`**

```js
export const config = { runtime: 'edge' };

export default async function handler(req) {
  if (req.method !== 'POST') {
    return new Response(JSON.stringify({ error: 'Method not allowed' }), {
      status: 405,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  let body;
  try {
    body = await req.json();
  } catch {
    return new Response(JSON.stringify({ error: 'JSON invalide' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const { prenom1, prenom2, email, telephone, date_mariage, message } = body;

  if (!prenom1 || !email) {
    return new Response(JSON.stringify({ error: 'prenom1 et email sont requis' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  // 1. Insérer dans Supabase
  const supabaseRes = await fetch(
    `${process.env.SUPABASE_URL}/rest/v1/leads`,
    {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'apikey': process.env.SUPABASE_SERVICE_KEY,
        'Authorization': `Bearer ${process.env.SUPABASE_SERVICE_KEY}`,
        'Prefer': 'return=representation'
      },
      body: JSON.stringify({ prenom1, prenom2, email, telephone, date_mariage, message })
    }
  );

  if (!supabaseRes.ok) {
    const err = await supabaseRes.text();
    return new Response(JSON.stringify({ error: 'Erreur Supabase', detail: err }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const [lead] = await supabaseRes.json();

  // 2. Envoyer email PM via Resend
  const dateLabel = date_mariage || 'non précisée';
  await fetch('https://api.resend.com/emails', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${process.env.RESEND_API_KEY}`
    },
    body: JSON.stringify({
      from: `Wedoria Studio <${process.env.FROM_EMAIL}>`,
      to: process.env.PM_EMAIL,
      subject: `🎉 Nouveau lead — ${prenom1}${prenom2 ? ' & ' + prenom2 : ''} (${dateLabel})`,
      text: [
        `Nouveau contact reçu sur le formulaire vitrine.`,
        ``,
        `Prénom(s) : ${prenom1}${prenom2 ? ' & ' + prenom2 : ''}`,
        `Email : ${email}`,
        `Téléphone : ${telephone || 'non renseigné'}`,
        `Date de mariage : ${dateLabel}`,
        ``,
        `Message :`,
        message || '(aucun message)',
        ``,
        `→ Ouvrir l'onboarding : https://wedoria.studio/onboarding/`
      ].join('\n')
    })
  });

  return new Response(JSON.stringify({ success: true, lead_id: lead.id }), {
    status: 200,
    headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' }
  });
}
```

- [ ] **Step 2 : Commit**

```bash
git add api/contact.js
git commit -m "feat: add /api/contact edge function"
```

```json:metadata
{"files": ["wedoria-site-vitrine/api/contact.js"], "verifyCommand": "curl -X POST http://localhost:3000/api/contact -H 'Content-Type: application/json' -d '{\"prenom1\":\"Test\",\"email\":\"test@test.com\"}'", "acceptanceCriteria": ["POST valide → 200 + lead_id", "POST sans email → 400", "lead inséré dans Supabase", "email PM reçu"], "requiresUserVerification": false}
```

---

### Task 2 : Créer `/api/onboarding.js`

**Goal:** Edge Function qui reçoit le brief complet du wizard, insère un projet dans Supabase, met à jour le statut du lead, et envoie l'email d'onboarding au client.

**Files:**
- Create: `wedoria-site-vitrine/api/onboarding.js`

**Acceptance Criteria:**
- [ ] POST avec brief complet → 200 + `{ success: true, projet_id }`
- [ ] POST sans `email_client` → 400
- [ ] Projet inséré dans `projets`
- [ ] `leads.statut` mis à `'signé'` si `lead_id` fourni
- [ ] Email reçu par le client avec template, formule, date

**Verify:** `curl -X POST /api/onboarding -d '{"prenom1":"Sophie","prenom2":"Thomas","email_client":"test@test.com","template":"romantique","formule":"premium","date_mariage":"2026-09-15"}' → {"success":true}`

**Steps:**

- [ ] **Step 1 : Créer `wedoria-site-vitrine/api/onboarding.js`**

```js
export const config = { runtime: 'edge' };

const DELAIS = { essentielle: '3 jours', premium: '5 jours', 'sur-mesure': '10 jours' };

export default async function handler(req) {
  if (req.method !== 'POST') {
    return new Response(JSON.stringify({ error: 'Method not allowed' }), {
      status: 405,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  let body;
  try {
    body = await req.json();
  } catch {
    return new Response(JSON.stringify({ error: 'JSON invalide' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const {
    lead_id, prenom1, prenom2, email_client,
    date_mariage, template, formule,
    couleur_primaire, couleur_secondaire, police,
    lieu, programme, infos_pratiques
  } = body;

  if (!prenom1 || !email_client) {
    return new Response(JSON.stringify({ error: 'prenom1 et email_client sont requis' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const headers = {
    'Content-Type': 'application/json',
    'apikey': process.env.SUPABASE_SERVICE_KEY,
    'Authorization': `Bearer ${process.env.SUPABASE_SERVICE_KEY}`,
    'Prefer': 'return=representation'
  };

  // 1. Insérer dans projets
  const projetRes = await fetch(`${process.env.SUPABASE_URL}/rest/v1/projets`, {
    method: 'POST',
    headers,
    body: JSON.stringify({
      lead_id: lead_id || null,
      prenom1, prenom2, email_client, date_mariage,
      template, formule, couleur_primaire, couleur_secondaire,
      police, lieu, programme, infos_pratiques
    })
  });

  if (!projetRes.ok) {
    const err = await projetRes.text();
    return new Response(JSON.stringify({ error: 'Erreur Supabase projets', detail: err }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const [projet] = await projetRes.json();

  // 2. Mettre à jour statut du lead si lead_id fourni
  if (lead_id) {
    await fetch(`${process.env.SUPABASE_URL}/rest/v1/leads?id=eq.${lead_id}`, {
      method: 'PATCH',
      headers,
      body: JSON.stringify({ statut: 'signé' })
    });
  }

  // 3. Envoyer email client
  const prenoms = prenom2 ? `${prenom1} et ${prenom2}` : prenom1;
  const dateLabel = date_mariage || 'date à confirmer';
  const delai = DELAIS[formule] || '5 jours';

  await fetch('https://api.resend.com/emails', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${process.env.RESEND_API_KEY}`
    },
    body: JSON.stringify({
      from: `Wedoria Studio <${process.env.FROM_EMAIL}>`,
      to: email_client,
      subject: `Votre site de mariage — on démarre ! ✨`,
      text: [
        `Bonjour ${prenoms},`,
        ``,
        `Votre brief a bien été reçu — merci pour ces informations !`,
        ``,
        `Voici ce qu'on a retenu :`,
        ``,
        `  Template choisi : ${template || 'à confirmer'}`,
        `  Formule : ${formule || 'à confirmer'}`,
        `  Date de mariage : ${dateLabel}`,
        `  Lieu : ${lieu || 'à confirmer'}`,
        ``,
        `Nous allons maintenant créer votre site sur cette base.`,
        `Délai de livraison : ${delai}`,
        ``,
        `Si vous avez des ajustements ou des précisions, répondez simplement à cet email.`,
        ``,
        `À très vite !`,
        `L'équipe Wedoria Studio`
      ].join('\n')
    })
  });

  return new Response(JSON.stringify({ success: true, projet_id: projet.id }), {
    status: 200,
    headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' }
  });
}
```

- [ ] **Step 2 : Commit**

```bash
git add api/onboarding.js
git commit -m "feat: add /api/onboarding edge function"
```

```json:metadata
{"files": ["wedoria-site-vitrine/api/onboarding.js"], "verifyCommand": "curl -X POST http://localhost:3000/api/onboarding -H 'Content-Type: application/json' -d '{\"prenom1\":\"Sophie\",\"email_client\":\"test@test.com\",\"template\":\"romantique\",\"formule\":\"premium\"}'", "acceptanceCriteria": ["POST valide → 200 + projet_id", "POST sans email_client → 400", "projet inséré dans Supabase", "leads.statut mis à jour si lead_id fourni", "email client reçu"], "requiresUserVerification": false}
```

---

### Task 3 : Créer `/api/delivery.js`

**Goal:** Edge Function qui marque un projet comme livré dans Supabase et envoie l'email de confirmation au client.

**Files:**
- Create: `wedoria-site-vitrine/api/delivery.js`

**Acceptance Criteria:**
- [ ] POST avec `projet_id` + `url_site` → 200 + `{ success: true }`
- [ ] POST sans `projet_id` → 400
- [ ] `projets.statut` mis à `'livré'`, `url_site` et `livre_at` remplis
- [ ] Email de livraison reçu par le client

**Verify:** `curl -X POST /api/delivery -d '{"projet_id":"uuid","url_site":"https://sophie-thomas.wedoria.fr"}' → {"success":true}`

**Steps:**

- [ ] **Step 1 : Créer `wedoria-site-vitrine/api/delivery.js`**

```js
export const config = { runtime: 'edge' };

export default async function handler(req) {
  if (req.method !== 'POST') {
    return new Response(JSON.stringify({ error: 'Method not allowed' }), {
      status: 405,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  let body;
  try {
    body = await req.json();
  } catch {
    return new Response(JSON.stringify({ error: 'JSON invalide' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const { projet_id, url_site } = body;

  if (!projet_id || !url_site) {
    return new Response(JSON.stringify({ error: 'projet_id et url_site sont requis' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const supabaseHeaders = {
    'Content-Type': 'application/json',
    'apikey': process.env.SUPABASE_SERVICE_KEY,
    'Authorization': `Bearer ${process.env.SUPABASE_SERVICE_KEY}`,
    'Prefer': 'return=representation'
  };

  // 1. Récupérer le projet (pour email_client, prenom1, prenom2, formule)
  const getRes = await fetch(
    `${process.env.SUPABASE_URL}/rest/v1/projets?id=eq.${projet_id}&select=*`,
    { headers: supabaseHeaders }
  );

  if (!getRes.ok) {
    return new Response(JSON.stringify({ error: 'Projet introuvable' }), {
      status: 404,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const projets = await getRes.json();
  if (!projets.length) {
    return new Response(JSON.stringify({ error: 'Projet introuvable' }), {
      status: 404,
      headers: { 'Content-Type': 'application/json' }
    });
  }

  const projet = projets[0];

  // 2. Mettre à jour le projet
  await fetch(
    `${process.env.SUPABASE_URL}/rest/v1/projets?id=eq.${projet_id}`,
    {
      method: 'PATCH',
      headers: supabaseHeaders,
      body: JSON.stringify({ statut: 'livré', url_site, livre_at: new Date().toISOString() })
    }
  );

  // 3. Envoyer email de livraison
  const prenoms = projet.prenom2 ? `${projet.prenom1} et ${projet.prenom2}` : projet.prenom1;
  const isPremium = ['premium', 'sur-mesure'].includes(projet.formule);

  await fetch('https://api.resend.com/emails', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${process.env.RESEND_API_KEY}`
    },
    body: JSON.stringify({
      from: `Wedoria Studio <${process.env.FROM_EMAIL}>`,
      to: projet.email_client,
      subject: `Votre site est en ligne ! 🌹`,
      text: [
        `Bonjour ${prenoms},`,
        ``,
        `Votre site de mariage est prêt — et il est magnifique.`,
        ``,
        `→ Voir votre site : ${url_site}`,
        ``,
        `Pour partager le lien avec vos invités, copiez simplement cette adresse :`,
        url_site,
        ``,
        `Ce que vos invités trouveront :`,
        `  - Les informations pratiques (lieu, horaires, programme)`,
        `  - Le compte à rebours jusqu'au grand jour`,
        ...(isPremium ? ['  - Le formulaire RSVP', '  - La galerie photos'] : []),
        ``,
        `Besoin d'une modification ? Répondez à cet email, on s'en occupe.`,
        ``,
        `Félicitations pour votre mariage — on est ravis d'en faire partie !`,
        ``,
        `L'équipe Wedoria Studio`
      ].join('\n')
    })
  });

  return new Response(JSON.stringify({ success: true }), {
    status: 200,
    headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' }
  });
}
```

- [ ] **Step 2 : Commit**

```bash
git add api/delivery.js
git commit -m "feat: add /api/delivery edge function"
```

```json:metadata
{"files": ["wedoria-site-vitrine/api/delivery.js"], "verifyCommand": "curl -X POST http://localhost:3000/api/delivery -H 'Content-Type: application/json' -d '{\"projet_id\":\"uuid\",\"url_site\":\"https://test.wedoria.fr\"}'", "acceptanceCriteria": ["POST valide → 200", "POST sans projet_id → 400", "projets.statut mis à 'livré'", "email livraison reçu par client"], "requiresUserVerification": false}
```

---

### Task 4 : Câbler le formulaire vitrine sur `/api/contact`

**Goal:** Modifier `wedoria-site-vitrine/index.html` pour que la soumission du formulaire contact appelle `/api/contact` au lieu d'une action par défaut.

**Files:**
- Modify: `wedoria-site-vitrine/index.html`

**Acceptance Criteria:**
- [ ] Soumission du formulaire → requête POST vers `/api/contact`
- [ ] Message de succès affiché après soumission
- [ ] Message d'erreur affiché si l'API échoue
- [ ] Bouton désactivé pendant l'envoi (pas de double submit)

**Verify:** Ouvrir la vitrine → remplir le formulaire → soumettre → voir message de succès → vérifier dans Supabase Table Editor que le lead est apparu

**Steps:**

- [ ] **Step 1 : Lire le formulaire existant dans `index.html`**

Repérer l'élément `<form>` du formulaire contact (chercher `#contact` ou `form`). Identifier les `name` de chaque champ input.

- [ ] **Step 2 : Ajouter un `id` sur le formulaire si absent**

Ajouter `id="contact-form"` sur le `<form>`.

- [ ] **Step 3 : Ajouter le script de soumission avant `</body>`**

```html
<script>
  document.getElementById('contact-form').addEventListener('submit', async function(e) {
    e.preventDefault();
    const btn = this.querySelector('[type="submit"]');
    const data = Object.fromEntries(new FormData(this));
    btn.disabled = true;
    btn.textContent = 'Envoi en cours…';

    try {
      const res = await fetch('/api/contact', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
      });
      const json = await res.json();
      if (res.ok) {
        this.innerHTML = '<p style="text-align:center;padding:2rem;font-family:\'Cormorant Garamond\',serif;font-size:1.2rem;">Merci ! Nous vous répondons dans la journée. ✨</p>';
      } else {
        throw new Error(json.error || 'Erreur');
      }
    } catch (err) {
      btn.disabled = false;
      btn.textContent = 'Envoyer';
      alert('Une erreur est survenue : ' + err.message + '. Réessayez ou écrivez-nous directement.');
    }
  });
</script>
```

- [ ] **Step 4 : Vérifier que les `name` des champs correspondent au payload attendu**

Les champs doivent avoir les `name` : `prenom1`, `prenom2`, `email`, `telephone`, `date_mariage`, `message`. Ajuster si nécessaire.

- [ ] **Step 5 : Commit**

```bash
git add index.html
git commit -m "feat: wire contact form to /api/contact"
```

```json:metadata
{"files": ["wedoria-site-vitrine/index.html"], "verifyCommand": "", "acceptanceCriteria": ["formulaire POST vers /api/contact", "message succès affiché", "bouton désactivé pendant envoi"], "requiresUserVerification": false}
```

---

### Task 5 : Câbler le wizard onboarding sur `/api/onboarding`

**Goal:** Modifier `wedoria-site-vitrine/onboarding/index.html` pour que la soumission finale du wizard appelle `/api/onboarding`.

**Files:**
- Modify: `wedoria-site-vitrine/onboarding/index.html`

**Acceptance Criteria:**
- [ ] Soumission du wizard → requête POST vers `/api/onboarding`
- [ ] Écran de confirmation affiché après succès
- [ ] `projet_id` stocké en `localStorage` pour usage futur

**Verify:** Remplir le wizard complet → soumettre → voir l'écran de confirmation → vérifier dans Supabase que le projet est créé

**Steps:**

- [ ] **Step 1 : Lire l'onboarding existant**

Repérer comment la dernière étape du wizard soumet les données (chercher `submit`, `fetch`, ou `EmailJS`). Identifier l'objet de données construit.

- [ ] **Step 2 : Remplacer l'appel EmailJS (ou stub) par l'appel à `/api/onboarding`**

Trouver le bloc de soumission et le remplacer par :

```js
async function submitOnboarding(data) {
  const res = await fetch('/api/onboarding', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data)
  });
  const json = await res.json();
  if (!res.ok) throw new Error(json.error || 'Erreur');
  if (json.projet_id) localStorage.setItem('wedoria_projet_id', json.projet_id);
  return json;
}
```

- [ ] **Step 3 : Ajouter l'écran de confirmation si absent**

Après le succès de `submitOnboarding`, afficher :

```js
document.getElementById('wizard-container').innerHTML = `
  <div style="text-align:center;padding:4rem 2rem;font-family:'Cormorant Garamond',serif;">
    <h2 style="font-size:2rem;margin-bottom:1rem;">Votre brief est reçu ! ✨</h2>
    <p style="font-size:1.1rem;color:#6b5c4e;">Vérifiez votre boîte email — nous vous avons envoyé un récapitulatif.</p>
    <p style="margin-top:1rem;font-size:0.9rem;color:#999;">Livraison sous ${DELAIS[data.formule] || '5 jours'}</p>
  </div>
`;
```

- [ ] **Step 4 : S'assurer que `data` contient bien `lead_id` si disponible**

```js
const lead_id = localStorage.getItem('wedoria_lead_id') || null;
const data = { lead_id, prenom1, prenom2, email_client, /* ... */ };
```

- [ ] **Step 5 : Commit**

```bash
git add onboarding/index.html
git commit -m "feat: wire onboarding wizard to /api/onboarding"
```

```json:metadata
{"files": ["wedoria-site-vitrine/onboarding/index.html"], "verifyCommand": "", "acceptanceCriteria": ["wizard POST vers /api/onboarding", "écran confirmation affiché", "projet_id stocké en localStorage"], "requiresUserVerification": false}
```

---

### Task 6 : Configurer les variables d'environnement sur Vercel

**Goal:** Ajouter les 5 variables d'environnement dans le dashboard Vercel pour que les Edge Functions fonctionnent en production.

**Files:** Aucun fichier modifié — configuration Vercel uniquement.

**Acceptance Criteria:**
- [ ] Les 5 variables présentes dans Vercel → Settings → Environment Variables
- [ ] Scope : Production + Preview
- [ ] Redéploiement déclenché après ajout

**Verify:** Vercel dashboard → Settings → Environment Variables → voir les 5 variables listées

**Steps:**

- [ ] **Step 1 : Ouvrir Vercel → projet `wedoria-site-vitrine` → Settings → Environment Variables**

- [ ] **Step 2 : Ajouter les variables une par une**

| Nom | Valeur |
|-----|--------|
| `SUPABASE_URL` | `https://xxxx.supabase.co` |
| `SUPABASE_SERVICE_KEY` | `eyJhbGc...` (service role key) |
| `RESEND_API_KEY` | `re_xxxx` |
| `PM_EMAIL` | `vu.nhusao@gmail.com` |
| `FROM_EMAIL` | `contact@wedoria.fr` |

- [ ] **Step 3 : Configurer le domaine expéditeur dans Resend**

Resend dashboard → Domains → Add domain `wedoria.fr` → ajouter les DNS records → vérifier.

- [ ] **Step 4 : Redéployer**

```bash
git commit --allow-empty -m "chore: trigger redeploy after env vars"
git push
```

```json:metadata
{"files": [], "verifyCommand": "", "acceptanceCriteria": ["5 variables présentes sur Vercel", "domaine wedoria.fr vérifié dans Resend", "redéploiement effectué"], "requiresUserVerification": false}
```

---

### Task 7 : Test E2E complet

**Goal:** Valider le flux complet de bout en bout : formulaire vitrine → Supabase → email PM → onboarding → email client → livraison → email livraison.

**Files:** Aucun fichier modifié.

**Acceptance Criteria:**
- [ ] Les 10 étapes du plan de test passent toutes

**Verify:** Checklist manuelle des 10 étapes ci-dessous

**Steps:**

- [ ] **Test 1 — Soumettre le formulaire vitrine (champs complets)**
  - Ouvrir `https://wedoria.vercel.app`
  - Remplir : Sophie, Thomas, email test, téléphone, date 2026-09-15, message
  - Soumettre → message de succès affiché
  - **Attendu :** Status 200, message de confirmation

- [ ] **Test 2 — Vérifier le lead dans Supabase**
  - Supabase → Table Editor → `leads`
  - **Attendu :** Nouveau row avec `statut = 'nouveau'`

- [ ] **Test 3 — Vérifier l'email PM**
  - Ouvrir `vu.nhusao@gmail.com`
  - **Attendu :** Email reçu dans les 30 secondes, objet "🎉 Nouveau lead — Sophie & Thomas (2026-09-15)"

- [ ] **Test 4 — Soumettre sans email (validation)**
  - Laisser le champ email vide → soumettre
  - **Attendu :** Validation HTML ou message d'erreur, pas de crash API

- [ ] **Test 5 — Remplir le wizard onboarding**
  - Ouvrir `https://wedoria.vercel.app/onboarding/`
  - Remplir toutes les étapes : Sophie & Thomas, romantique, premium, lieu, programme
  - Soumettre → écran de confirmation
  - **Attendu :** Status 200, message de confirmation affiché

- [ ] **Test 6 — Vérifier le projet dans Supabase**
  - Supabase → Table Editor → `projets`
  - **Attendu :** Nouveau row avec `template = 'romantique'`, `formule = 'premium'`, `statut = 'brief'`

- [ ] **Test 7 — Vérifier l'email client onboarding**
  - Ouvrir la boîte email du client test
  - **Attendu :** Email reçu, objet "Votre site de mariage — on démarre ! ✨", données correctes

- [ ] **Test 8 — Déclencher la livraison**

```bash
curl -X POST https://wedoria.vercel.app/api/delivery \
  -H "Content-Type: application/json" \
  -d '{"projet_id":"[copier le projet_id de Supabase]","url_site":"https://sophie-thomas.wedoria.fr"}'
```
  - **Attendu :** `{"success":true}`

- [ ] **Test 9 — Vérifier la mise à jour Supabase**
  - Supabase → `projets` → vérifier le row
  - **Attendu :** `statut = 'livré'`, `url_site` rempli, `livre_at` non null

- [ ] **Test 10 — Vérifier l'email de livraison**
  - Ouvrir la boîte email du client test
  - **Attendu :** Email reçu, objet "Votre site est en ligne ! 🌹", lien correct

```json:metadata
{"files": [], "verifyCommand": "", "acceptanceCriteria": ["10 tests E2E passent", "leads créé dans Supabase", "projets créé dans Supabase", "3 emails reçus (PM + client onboarding + client livraison)"], "requiresUserVerification": false}
```
